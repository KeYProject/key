\javaSource "src";

\functions {
    Heap h;
    Object o;
}

\predicates {
    b;
    p;
}

\problem {
    wellFormed(h) -> (p -> \if(b) \then(wellFormed(h)) \else(wellFormed(store(h, o, Test::$x, Z(5(#))))))
}