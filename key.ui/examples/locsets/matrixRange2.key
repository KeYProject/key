\programVariables {
  int[][] m1;
  int[][] m2;
}

\problem {
    m1 != null & m2 != null & m1!=m2 & m1.length > 10 & m1[0].length > 10 & m2.length > 10 & m2[0].length > 10 &
      wellFormedMatrix(m1, heap) & wellFormedMatrix(m2,heap)

      ->

      (
        intersect(matrixRange(heap, m1,0,m1.length, 0, m1[0].length), matrixRange(heap, m2,0,m2.length, 0, m2[0].length)) = empty
      )

}
