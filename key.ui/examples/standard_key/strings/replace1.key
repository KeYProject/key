\withOptions Strings:on;

\problem {

  \forall Seq str; \forall int c; 
            \forall int d; \forall int pos; 
                  (pos >= 0 & pos < seqLen(str) -> (seqGet<[int]>(clReplace(str, c, d), pos) = c -> c = d))

}