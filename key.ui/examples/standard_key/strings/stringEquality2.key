\withOptions Strings:on;


\problem {

  \forall Seq s;\forall Seq s2; \forall Seq s3; \forall int i; (i >= 0 & i + 5 <= seqLen(s) -> (seqSub(s, 0, i) = s2 & seqSub(s, i, i + 5) = s3 -> seqConcat(s2, s3) = seqSub(s, 0, i + 5)))

}