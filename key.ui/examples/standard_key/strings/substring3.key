/**
     Problem should be closed automatically. 

 */

\withOptions Strings:on;


\problem {
  \forall int i; 
   \forall Seq l; ((seqLen(l) >= i & i >= 0 & seqSub(l, 0, i) = l) -> seqLen(l) = i )

}
