// Input file for KeY standalone prover version 0.579
// the first induction proof with the KeY system
// documentation in decrToZero.tex

\programVariables {
	int i;
}

\problem{\forall int il; {i:=il} (geq(i,0)
  ->(( \<{while (i>0) i--;}\> i=0)))
}
