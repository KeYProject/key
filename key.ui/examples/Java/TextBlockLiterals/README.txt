example.name = Text Block Literals
example.path = Java 25
example.file = project.key
example.additionalFile.1 = TextBlockLiterals.java

Text Block Literals are supported in KeY. They are reduced to normal String literals.

The proof obligation is verifiable without user interaction.
