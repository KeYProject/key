\programVariables {
  int i;
  int j;
  int k;
  int[] a;
  int[] b;
  int sum;
  int x;
  boolean cond;
  int M;
  int N;
}

\problem {
wellFormed(heap) & a!=null &
noW(arrayRange(a,0,a.length)) & noR(arrayRange(a,0,a.length)) & a.length>10
->{i:=0}
\<{while (i<a.length-1) {
    if(i > (a.length-1)/2){
        a[i] = a[i+1];
    }
    else {
        a[i] = 0;
    }
    ; // this is just a comment, the semicolon is replaced by a merge_point(i);
    //@ merge_proc \MergeByIfThenElseAntecedent\;
    i++;}
}\>true
}