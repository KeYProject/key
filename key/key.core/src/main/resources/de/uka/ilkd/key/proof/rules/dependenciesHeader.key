\functions{
 int anonLength(int);
}

\predicates {
  \nonRigid noRaW(LocSet);
  \nonRigid noWaR(LocSet);
  \nonRigid noWaW(LocSet);
  \nonRigid noR(LocSet);
  \nonRigid noW(LocSet);
  \nonRigid rPred(LocSet, int);
  \nonRigid wPred(LocSet, int);
  \nonRigid noRaWAtHistory(LocSet, int);
  \nonRigid noWaRAtHistory(LocSet, int);
  \nonRigid noWaWAtHistory(LocSet, int);
  \nonRigid noRAtHistory(LocSet, int);
  \nonRigid noWAtHistory(LocSet, int);
  \nonRigid relaxedNoRaW(LocSet, LocSet, LocSet, LocSet);
  \nonRigid relaxedNoWaR(LocSet, LocSet, LocSet, LocSet);
  \nonRigid relaxedNoWaW(LocSet, LocSet, LocSet);
}

\programVariables {
	int timestamp;
	int uniqueMarker;
}
