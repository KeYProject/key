### KeY

\problem {
  \forall int i; (i >= 0 -> 2*i >= i)
}

### contains.1

(assert (not (forall ((var_i U)) (=> (instanceof var_i sort_int) (=> (>= (u2i var_i) 0) (>= (* 2 (u2i var_i)) (u2i var_i)))))))

### expected

valid