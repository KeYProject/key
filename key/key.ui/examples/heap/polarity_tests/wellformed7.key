\javaSource "src";

\functions {
    Heap h;
    Object o;
}

\predicates {
    b;
    p;
}

\problem {
    wellFormed(h) -> (p -> \if(b) \then(wellFormed(store(h, o, Test::$x, Z(5(#))))) \else(wellFormed(h)) )
}