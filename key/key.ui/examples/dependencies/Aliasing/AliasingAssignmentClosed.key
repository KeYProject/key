/*
Here we do not have aliasing so o and p are two different object. So there is noRaW.
It Supposed to be closed.
Current State: Closed.
*/
\javaSource "src/";

\predicates{
  
}

\programVariables {
  A o;
  A p;
  int v;
}

\problem {

  o != null & p != null & o!=p & noW(singleton(o, A::$a)) & noRaW(singleton(o, A::$a)) -> \<{ o.a = p.a ; v = p.a; }\>noRaW(singleton(o, A::$a)) 
}