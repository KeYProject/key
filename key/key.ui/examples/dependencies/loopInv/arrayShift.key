//\javaSource "";

\predicates{
  
}

\programVariables {
  int[] a;
  int i;
}


\problem {
	 a!=null & wellFormed(heap)
			 -> {i:=0} \<{
				 /*@ loop_invariant i>=0 && i < a.length-1 &&
				  @ noR(a[0]) && noW(a[a.length - 1]) &&
				  @ (\forall int j; j >= 0 && j < a.length -1; noRaW(a[j]));
				  @ decreases length - i;
				  @ assignable a[0..a.length-2];
				  @*/
                  while(i < a.length-1){
   					a[i]=a[i+1];
   					i++;
   				} 
   			}\>(noRaW(allFields(a))) 
}