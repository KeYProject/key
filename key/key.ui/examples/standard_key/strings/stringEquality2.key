\withOptions Strings:on;


\problem {

  \forall CharList s;\forall CharList s2; \forall CharList s3; \forall int i; (i >= 0 & i + 5 <= clLength(s) -> (clSub(0, i, s) = s2 & clSub(i, i + 5, s) = s3 -> clConcat(s2, s3) = clSub(0, i + 5, s)))

}