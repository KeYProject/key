\withOptions Strings:on;

\functions {
   int i; int j; int j2;
   Seq s1; Seq t1;  
}

\problem {
 
  (i >= 0 & i <= j & j <= seqLen(s1) & j2 >= i & j2 <= j & seqSub(s1, i, j) = t1) -> clStartsWith(t1, seqSub(s1, i, j2))

}
