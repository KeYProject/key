\functions {
   //These are just program variables. 
   //General non-rigid functions are unnecessary in this setting!
   \nonRigid[Location] Object o1;
   \nonRigid[Location] Object o2;

   //symbols representing fields
   \unique Field f;
   \unique Field g;
}

\problem {
    o1 != o2
    ->
    //{o1.f := 1; o2.f := 2; o1.g = 2}(o1.f = 1)
    {heap:=store(heap,o1,f,1)} {heap:=store(heap,o2,f,2)} {heap:=store(heap,o1,g,2)} \[{}\](int)select(heap,o1,f)=1
}  