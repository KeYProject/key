\functions {
   int startVal;
}

\programVariables {
  int timer;
}

/* 

  Invariant:
        timer >= 0
  Decreasing term:
        timer
*/

\hoareTotal {

{ startVal >= 0 }
 
[timer := startVal]

\[{   
      while (timer>0) {
          timer = timer -1;
      } 	
}\]

{
   timer = 0
}
 
}
