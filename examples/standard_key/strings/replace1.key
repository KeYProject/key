\withOptions Strings:on;

\problem {

  \forall CharList str; \forall int c; 
            \forall int d; \forall int pos; 
                  (pos >= 0 & pos < clLength(str) -> (clCharAt(pos, clReplace(c, d, str)) = c -> c = d))

}