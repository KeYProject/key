\withOptions Strings:on;


\problem{
\forall int start; \forall int end;
  \forall CharList l; ( start >=0 & start <= end & end >=0 & end <= clLength(l) -> (clSub(start, end, l) = "" -> start = end ))

}