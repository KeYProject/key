/**
     Problem should be closed automatically. 

 */

\withOptions Strings:on;


\problem {
  \forall int i; 
   \forall CharList l; ((clLength(l) >= i & i >= 0 & clSub(0, i, l) = l) -> clLength(l) = i )

}
