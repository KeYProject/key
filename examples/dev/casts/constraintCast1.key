\javaSource "classes/";

\programVariables {
 A a;
 B b;
}

// equation should be unifiable using meta variables
// (trivial for giving a concrete instantiation namely null)
// D is not a subclass of A (nor vice versa)

\problem {
 \exists A a;\exists D d; (a=d)
}
