\javaSource "classes/";

\functions {
   int f(A);
   int g(B);
}

\programVariables {
 A a;
 B b;
}

// no casts for arguments of f should be inserted
// casts for arguments of g should be inserted once

\problem {
 a = b -> (f(a) = f(b) & 0 = g(b))
}
