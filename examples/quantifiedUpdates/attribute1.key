\javaSource "attribute_java/";

\functions {
	int f(A);
}

\problem {
	\<{ A obj; }\> (
            !obj = null ->
            { \for A o; o.a@(A) := f(o) }
              \<{ ++obj.a; }\>
              obj.a@(A) = add(f(obj), 1)
        )
}
