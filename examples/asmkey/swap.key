functions {
    dynamic int i;
    dynamic int j;
}

procedures {
    swap(x:int, y:int) { x := y, y := x };
}

problem {
    all x:int. all y:int. 
	(x = i & y = j 
         -> 
         < call swap(i, j) > (j = x & i = y))
}
