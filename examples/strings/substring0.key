/**
     Problem should be closed automatically. 

     Ideally: The strategies do not actually compute the value of the substring expression, 
     but use the knowledge that 

          substring(0, length(str)) = str 

     holds for all strings.
 */

\problem {

   substring(0, length("hallo"), "hallo") = "hallo" 

}
