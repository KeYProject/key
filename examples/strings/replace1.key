\problem {

  \forall CharList str; \forall jchar c; 
            \forall jchar d; \forall int pos; 
                  (pos >= 0 & pos < length(str) -> (charAt(pos, replace(c, d, str))=c -> c = d))

}