\problem {

\forall CharList l; \forall CharList subL; \forall int start; \forall int end; \forall int pos; (
 start >= 0 & start <= end & end <= length(l) & start <= pos & pos < start-end & subL = substring(start, end, l)
->
 charAt(pos + start, l) = charAt(pos, subL) 
)


}