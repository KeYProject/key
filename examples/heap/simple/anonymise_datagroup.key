\functions {
   Object o;
   \unique Field f;
   
   Set dataGroup(Heap);
   
   Heap heapAtPre;
   Heap anonHeap;   
   Set modAtPre;
}


\problem {
    //We anonymise the modifies clause "{dataGroup}", 
    //where "o2.*" is disjoint from the data group
    
    int::select(heap, o, f) = 33
    & disjoint(allFields(o), dataGroup(heap))
    & heap = heapAtPre
    & modAtPre = dataGroup(heap)
    
    ->
    
    ({heap := changeHeapAtLocs(heap, modAtPre, anonHeap)} int::select(heap, o, f)) = 33
}  