\functions {
  Object myObj;
  Object myObj2;
  \unique Field myField; 
}

\predicates {
  p;
}

\problem {
    any::select(heap, myObj, myField) = myObj2 
    -> reach(heap, singleton(myField), myObj, myObj2, 1)
}
