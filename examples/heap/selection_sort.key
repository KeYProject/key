\javaSource "src";

\programVariables {
   MyClass self;
   int[] a;
}

\functions {
   Heap heapAtPre;
}


\problem {
   self != null 
   & self.<created> = TRUE
   & a != null
   & a.<created> = TRUE
   & wellFormed(heap) 
   & heap = heapAtPre 
   
   -> \[{ MyClass.selectionSort(a)@MyClass;}\]
   
     \forall int x; \forall int y; (0 < x & x < y & y < a.length -> a[x] <= a[y])
}
