\programVariables {
  Object o;
}

\functions {
  \unique Field f;
  \unique Field g;
}

\problem {
  //{heap:=store(heap,o,f,1)} {heap:=store(heap,o,g,2)}  \[{}\] int::select(heap,o,f)=1
  
  subset(singleton(pair(o, f)), setComprehension{Field fv;}(allLocs, pair(o,fv)))
}
