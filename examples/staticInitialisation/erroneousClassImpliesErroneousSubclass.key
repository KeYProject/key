/*
  @provable automatic

      Starting from a legal heap structure (inReachableState) one can derive that if a
   class is marked as erroneous then a subclass cannot be initialized.
  
*/

\classpath "classpath";
\javaSource "java/";

\problem {
     (inReachableState & A.<classErroneous> = TRUE  -> B.<classInitialized> = FALSE)
}

