\functions{
 int anonLength(int);
}

\predicates {
  \nonRigid noRaW(LocSet);
  \nonRigid noWaR(LocSet);
  \nonRigid noWaW(LocSet);
  \nonRigid noR(LocSet);
  \nonRigid noW(LocSet);
  \nonRigid rPred(LocSet, int);
  \nonRigid wPred(LocSet, int);
  \nonRigid relaxedRPred(LocSet, int);
  \nonRigid relaxedWPred(LocSet, int);
  \nonRigid relaxedNoReads(LocSet);
  \nonRigid relaxedNoWrites(LocSet);
  \nonRigid noRaWAtHistory(LocSet, int);
  \nonRigid noWaRAtHistory(LocSet, int);
  \nonRigid noWaWAtHistory(LocSet, int);
  \nonRigid noRAtHistory(LocSet, int);
  \nonRigid noWAtHistory(LocSet, int);
  \nonRigid relaxedNoR(LocSet);
  \nonRigid relaxedNoW(LocSet);
  \nonRigid relaxedNoRaW(LocSet, LocSet, LocSet, LocSet);
  \nonRigid relaxedNoWaR(LocSet, LocSet, LocSet, LocSet);
  \nonRigid relaxedNoWaW(LocSet, LocSet, LocSet);
  \nonRigid relaxedNoRAtHistory(LocSet, int);
  \nonRigid relaxedNoWAtHistory(LocSet, int);
  \nonRigid relaxedNoRaWAtHistory(LocSet, int);
  \nonRigid relaxedNoWaRAtHistory(LocSet, int);
  \nonRigid relaxedNoWaWAtHistory(LocSet, int);

}

\programVariables {
	int timestamp;
}
