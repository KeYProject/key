# Used to identify and hunt a bug.
# Raised a -1 illegal index exception
key: |  
  \problem { ==> 1 + 0 + 0 + 0 + 0 >= 0 }
script: |
  rule add_zero_right on:(?focus(?) + ? + ? + ?);
goals:
  - ==> 1 + 0 + 0 + 0 >= 0