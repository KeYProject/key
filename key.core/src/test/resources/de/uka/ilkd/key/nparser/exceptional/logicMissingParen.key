// verbose: true
// exceptionClass: ParseCancellationException
// msgContains: ')' (closing parenthesis) expected, but found '}'

\problem {
   (true & true
}

// A logic-level (non-modality) syntax error: the closing parenthesis of the
// formula is missing. The message should name the missing token concretely
// instead of dumping a generic list of expected token types.
