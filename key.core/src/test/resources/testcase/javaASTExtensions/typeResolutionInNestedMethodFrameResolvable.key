/**
 * The formula below must be parsed successfully.
 * The reason is that the inner method-frame redirects the scope to
 * class Other in package sub2 where class Third should be found.
 * The outer method-frame redirects to a class in package sub1
 * thus ensuring the type TestJavaCardDLExtensions is resolvable.
 */
 \javaSource "src/";

\problem {

  \<{  method-frame(source=setSize(int)@sub1.TestJavaCardDLExtensions) {
          method-frame(source=setSize(int)@sub2.Other) {
            Third three = null;
          }
          TestJavaCardDLExtensions t = null;
       }
  }\> true

}
