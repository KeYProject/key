/**
 * The formula below must *not* be parsed since
 * the type "TestJavaCardDLExtensions" of local variable "t"
 * should not be resolvable.
 * The reason is that the code in the modality is parsed inside the
 * default package, where no class "TestJavaCardDLExtensions" exists
 * (it is located in package sub1).
 */
 \javaSource "src/";

\problem {

  \<{ TestJavaCardDLExtensions t = null; }\> true

}
