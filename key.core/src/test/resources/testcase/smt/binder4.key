\predicates {
   binder{true}(int);
}

\functions {
     int f(int);
}

\problem {
   binder{int x;}(f(x)) -> binder{int y;}(f(y))
}

/*

   This should be the case!

   Alpha renaming.
   Probably not worth the extra work this would need.
   This test case could remain open for now.
*/
