// Naming harness (#3851): hiding a formula adds a goal-local insert_hidden taclet
// (the \addrules channel). The added rule must live in the goal's local rule set,
// must not be visible to sibling branches, and pruning must remove it.
\predicates {
  a;
  b;
}

\problem { (a | !a) & (b -> b) }
