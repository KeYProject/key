<?xml version="1.0" encoding="UTF-8"?>

<profile name="JML Reference" id="org.key_project.jmlediting.profile.jmlref">
	
	<behavior_spec>
		<keyword name="behavior"/>
		<keyword name="behaviour"/>
	</behavior_spec>
	
	<behavior_spec>
		<keyword name="normal_behavior"/>
		<keyword name="normal_behaviour"/>		
	</behavior_spec>
	
	<behavior_spec>
		<keyword name="exceptional_behavior"/>
		<keyword name="exceptional_behaviour"/>		
	</behavior_spec>

	<generic_spec keyword="required">
		<description>A requires clause specifies a precondition of method or constructor.</description>
	</generic_spec>
	<generic_spec keyword="ensures">
		<description>An ensures clause specifies a normal postcondition, i.e., a property that is guaranteed to hold at the end of the method (or constructor) invocation in the case that this method (or constructor) invocation returns without throwing an exception.</description>
	</generic_spec>
	<generic_spec keyword="assignable">
		<description>An assignable clause gives a frame axiom for a specification. It says that, from the client's point of view, only the locations named, and locations in the data groups associated with these locations, can be assigned to during the execution of the method. The values of all subexpressions used in assignable clauses, such as i-1 in a[i-1], are computed in the pre-state of the method, because the assignable clause only talks about locations that exist in the pre-state. </description>
	</generic_spec>
	
			
</profile>
