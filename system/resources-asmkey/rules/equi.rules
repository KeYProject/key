/* done */

taclets {

    // XXX: missing
    // nemo
    
    equi_skip_right {
        find ([#R par skip] #phi)
        replacewith ([#R] #phi)
        heuristics(simplify_asm)
    };
    
    equi_skip_left {
        find ([skip par #R] #phi)
        replacewith ([#R] #phi)
        heuristics(simplify_asm)
    };
    
    equi_com {
        find ([#R par #S] #phi)
        replacewith ([#S par #R] #phi)
    };
    
    equi_ass {
        find ([#R1 par (#R2 par #R3)] #phi)
        replacewith ([(#R1 par #R2) par #R3] #phi)
    };
    
    equi_idem {
        find ([#R par #R] #phi)
        replacewith ([#R] #phi)
        heuristics(simplify_asm)
    };
    
    equi_if1 {
        find ([if #psi then #R1 else #R2 end par #S] #phi)
        replacewith ([if #psi then #R1 par #S else #R2 par #S end] #phi)
    };
    
    equi_if2 {
        find ([#S par if #psi then #R1 else #R2 end] #phi)
        replacewith ([if #psi then #S par #R1 else #S par #R2 end] #phi)
    };



    equi_seq_skip1 {
        find ([#R seq skip] #phi)
        replacewith ([#R] #phi)
        heuristics(simplify_asm)
    };
    
    equi_seq_skip2 {
        find ([skip seq #R] #phi)
        replacewith ([#R] #phi)
        heuristics(simplify_asm)
    };
    
    equi_seq_ass {
        find ([(#R1 seq #R2) seq #R3] #phi)
        replacewith ([#R1 seq (#R2 seq #R3)] #phi)
    };
    
    equi_seq_if {
        find ([if #psi then #R1 else #R2 end seq #S] #phi)
        replacewith ([if #psi then #R1 seq #S else #R2 seq #S end] #phi)
    };

}    
