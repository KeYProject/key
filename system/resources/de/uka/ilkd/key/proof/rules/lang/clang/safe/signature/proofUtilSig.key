\predicates {
	// Rigid predicate such that isValidPtr(o) = isValidPtrSaved(o, storage(objBlock(o)),
	// which is never rewritten. Needed to reuse previous proofs of isValidPtr(o).
	// Ideally it should be a nonrigid predicate with dependencies.
	isValidPtrSaved(Object, StorageMode);
	
	// Rigid predicate such that objExists(o) = objExistsSaved(o, storage(objBlock(o)),
	// which is never rewritten. Needed to reuse previous proofs of objExists(o).
	// Ideally it should be a nonrigid predicate with dependencies.
	objExistsSaved(Object, StorageMode);	
}
