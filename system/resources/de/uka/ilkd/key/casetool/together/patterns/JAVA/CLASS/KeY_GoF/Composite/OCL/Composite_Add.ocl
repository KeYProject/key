patternmodification#
addMethod#add#Composite

mapping#Composite#'Composite'
mapping#add#'Name of method add in class Composite'
mapping#Component#'Component'
mapping#children#'Supplier Role'

postcondition#method#add#Composite

schema# 
	if (result=true) then (self.children->count(c)=1)
	else self.children->count(c)=self.children@pre->count(c)
	endif


comment#

